<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.facebook.drawee.view.SimpleDraweeView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        fresco:placeholderImage="@drawable/appleandroid" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/height_text_grid_item"
        android:layout_gravity="bottom"
        android:background="@color/black_overlay"
        android:orientation="vertical"
        android:padding="@dimen/general_16">

        <TextView
            android:id="@+id/textAppName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/margin_bottom_primary_text"
            android:text="@string/no_name"
            android:textColor="@android:color/white"
            android:textSize="@dimen/size_primary_text" />

        <TextView
            android:id="@+id/textAppAuthor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/no_author"
            android:textColor="@android:color/white"
            android:textSize="@dimen/size_secondary_text" />
    </LinearLayout>

</FrameLayout>